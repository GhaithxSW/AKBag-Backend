meta {
  name: Create Collection
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/collections
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "New Collection",
    "description": "A sample collection for testing"
  }
}
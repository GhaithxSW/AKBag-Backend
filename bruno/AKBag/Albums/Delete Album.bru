meta {
  name: Delete Album
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/albums/{{albumId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  albumId: 1
}